[package]
name = "rust-wrap"
version = "0.1.0"
edition = "2021"

[dependencies]
wrap = "0.5"
rusqlite = { version = "0.31", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dependencies]
